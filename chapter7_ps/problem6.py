#can we chnage the values inside a list which is contained in set
s ={8, 7,12, "Urvi", [1, 2]}

#we can't include list in set
#there is no indexing in set
#sets require all elements to be immutable and hashable